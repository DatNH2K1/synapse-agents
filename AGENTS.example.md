# AGENTS

## Rules
- MANDATORY: In the first turn of any conversation or session, you must view/read `~/.gemini/config/plugins/synapse-plugin/AGENTS.md` and check the rules listed there (e.g. under `synapse-plugin/rules/`) to ensure you always follow the latest rules and formatting requirements dynamically.
